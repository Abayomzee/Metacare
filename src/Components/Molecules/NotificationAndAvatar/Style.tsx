import styled from "styled-components";
import { FlexRowAiCenter } from "Styles/Abstract/Mixins";

export const NotificationAndAvatarStyle = styled.div`
  ${FlexRowAiCenter};
  gap: 4.8rem;
  border: 1px solid var(--color-10);
  border-radius: 0.8rem;
  padding: 1rem 2rem;
`;

import styled from "styled-components";

export const NotificationBadgeStyle = styled.span`
  display: grid;
  place-items: center;
  background-color: var(--color-5);
  color: var(--color-white);
  border-radius: 0.4rem;
  font-style: normal;
  font-weight: 400;
  font-size: 1.2rem;
  width: 1.6rem;
  height: 1.6rem;
`;

export const PerformanceBadgeStyle = styled.span`
  display: grid;
  place-items: center;
  background-color: var(--color-6);
  color: var(--color-7);
  border-radius: 0.4rem;
  font-style: normal;
  font-weight: 400;
  font-size: 1.2rem;
  padding: 0.56rem 1.68rem;
`;

import React from "react";
import { UserAvatarStyle } from "./Style";

interface Props {}
const UserAvatar: React.FC<Props> = (props) => {
  return <UserAvatarStyle></UserAvatarStyle>;
};

export default UserAvatar;
